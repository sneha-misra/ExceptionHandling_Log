note:

1. before running the log create a text file for writting the log and put the path  including filename  in nlog.config 'internalLogFile' attibute.

2. target tag add the file path in 'filename' attribute